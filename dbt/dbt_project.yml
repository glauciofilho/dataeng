name: nyc_taxi
version: "1.0.0"
config-version: 2

profile: nyc_taxi

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nyc_taxi:
    silver:
      +schema: silver
      +materialized: view
    gold:
      +schema: gold
      +materialized: table
      +file_format: parquet
